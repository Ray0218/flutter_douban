

import 'package:flutter/material.dart';

class KLUnKnowPage extends StatelessWidget {
  const KLUnKnowPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(

      appBar:AppBar(),
      body: Center(

        child:Text("未知页面")
      
    )
    );
  }
}